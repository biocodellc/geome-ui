import angular from 'angular';

import fimsDashboardMemberProjects from './member-projects.component';

export default angular
  .module('fims.dashboardMemberProjects', [])
  .component('fimsDashboardMemberProjects', fimsDashboardMemberProjects).name;
