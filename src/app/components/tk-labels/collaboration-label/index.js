import angular from 'angular';

import collaborationLabel from './collaboration.component';

export default angular
  .module('fims.collaborationLabel', [])
  .component('collaborationLabel', collaborationLabel).name;
