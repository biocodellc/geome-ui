import angular from 'angular';

import attributionLabel from './attribution.component';

export default angular
  .module('fims.attributionLabel', [])
  .component('attributionLabel', attributionLabel).name;
