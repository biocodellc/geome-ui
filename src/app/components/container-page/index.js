import angular from 'angular';

import containerPage from './container-page.component';

export default angular
  .module('fims.container-page', [])
  .component('fimsContainerPage', containerPage).name;
