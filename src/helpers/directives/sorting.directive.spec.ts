import { SortingDirective } from './sorting.directive';

describe('SortingDirective', () => {
  it('should create an instance', () => {
    const directive = new SortingDirective();
    expect(directive).toBeTruthy();
  });
});
